# Core data
pandas>=2.2
numpy>=1.26
pyarrow>=16.0

# NBA data source
nba_api>=1.5.2

# Config
PyYAML>=6.0

# Modeling (used from Stage 2 onward)
scikit-learn>=1.5
lightgbm>=4.3
scipy>=1.11  # Spearman gates (scripts/eval_rookies.py); already an sklearn dependency

# HTTP (nba_api dependency, pinned loosely for retries/timeouts)
requests>=2.31

# Web app backend (scripts/serve.py -> src/fantasy_nba/api); the frontend builds
# separately (cd frontend && npm install && npm run build)
fastapi>=0.115
uvicorn[standard]>=0.30

# Legacy local explorer (scripts/explore.py); bundles altair for charts
streamlit>=1.40

# DARKO live pull (scripts/pull_darko.py) — darko.app has no API and renders client-side, so we
# drive a headless browser. Needs a one-time browser install: `python -m playwright install chromium`.
# Only scripts/pull_darko.py imports it (lazily); models/darko.py reads the cached CSV without it.
playwright>=1.44
